# The "gold" — never commit weights / proprietary checkpoints
weights/
*.pt
*.pth
*.ckpt
*.safetensors
*.bin

# Raw audio datasets (manifests are tracked, raw audio is not)
data/raw/
data/**/audio/
# Local training artifacts & test audio (the "gold" stays off-git)
data/csmmlx_ds/
data/csmmlx_runs/
data/testes_maya/
data/meetings/
data/dataset_v1/
data/moshi_pairs/
data/voice clone eleven labs/
*.wav
*.flac
*.mp3
*.ogg

# Python
__pycache__/
*.py[cod]
.venv/
.venv*/
venv*/
.ipynb_checkpoints/
*.egg-info/

# Env / secrets
.env
.env.*
*.key

# Reference repos cloned for study — never vendor others' code
research/repos/

# Phase 0 throwaway spikes (code is disposable; keep only metrics in tech-stack.md)
phase0/**/venv*/
phase0/**/__pycache__/

# OS / editor
.DS_Store
.idea/
.vscode/

# dados locais de avaliação (working data; o artefato é o export feedback.jsonl)
tools/rate/ratings.jsonl
tools/rate/*.tmp
